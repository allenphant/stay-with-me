# Stay With Me 待辦清單

> **更新時間**：2026-09-17
> **維護方式**：本檔案維護產品待辦與階段目標；`CURRENT_STATE.md` 只記錄最近一次交接狀態。完成項目保留在「已完成」，並附上發布 PR 或驗收資訊。

## 狀態與優先級

- `P0`：阻塞主要使用流程
- `P1`：下一個主要產品體驗
- `P2`：重要改善，但有替代路徑
- `P3`：細節與長期優化
- `✅` 已完成　`⏳` 進行中　`⬜` 待辦　`🧭` 待決策

## 現在處理：共同計畫主軸的正式站驗收與下一批產品決策

- `P1` ⬜ 補正式站真人驗收：首次進入、側欄跳轉、行程新增

## 產品功能待辦

- `P1` ⬜ 每日共同問答
- `P1` ⬜ 代幣系統與天竺鼠飼料商店
- `P1` ⬜ AI 每週回顧
- `P2` ⬜ 每日小日記：每天一則，另一人以代幣解鎖
  - `🧭` 決定字數計價公式、最低價與上限
  - `🧭` 決定代幣是個人錢包還是共享錢包
  - `P0` 鎖定內容必須使用 Firestore rules 與可信任後端扣款／解鎖，不能只靠前端隱藏
  - `P2` 決定日記編輯後價格是否重新計算，以及解鎖是否永久有效
- `P2` 🧭 共享白板：共同保存可聊話題、自由文字與 Todo list
  - `🧭` 決定第一版採結構化區塊白板，或支援自由拖拉的無限畫布
  - `🧭` 決定白板數量、即時衝突處理，以及白板 Todo 是否同步到共同計畫

## 品質與可及性待辦

- `P3` ⬜ 拆分設定 modal，降低共享空間與 AI 設定的資訊密度

## 已完成

- `✅` 紀念日新增、刪除與未來 30 天提示
- `✅` 純行程 CRUD：標題、日期、時間、地點、備註
- `✅` 點日期格空白處新增預填日期的行程
- `✅` 點中央年月快速跳轉
- `✅` 共享日曆顯示行程、計畫卡與紀念日
- `✅` 手機 Planner 改為選定日期＋agenda 清單，避免小螢幕月曆橫向捲動
- `✅` Planner 顯示同步中／已同步／同步失敗與重試入口
- `✅` Planner 顯示共編者、最近同步時間與項目建立者
- `✅` 行程／紀念日刪除提供短暫 undo，紀念日可直接編輯
- `✅` 補齊 Planner、表單、API key、contenteditable 與 icon-only 控制的可及性標籤
- `✅` 為背景同步與未登入提交補上清楚的錯誤與復原流程
- `✅` 加入 `prefers-reduced-motion`，並補強鍵盤 focus 與 200% zoom 下的窄版布局
- `✅` 修正空的圖片 preview `src`／`alt` markup
- `✅` 固定版本 CDN 補上 SRI 與 `defer` 載入策略；Tailwind Play CDN、未固定 DragDropTouch 例外已明確註記
- `✅` 共同計畫移到首頁主內容第一個核心區塊，並加入側欄第一級入口與滾動高亮（PR #9）
- `✅` Planner 統一行程／待辦／願望／約會的新增與編輯入口：首頁按鈕、日期格、既有項目與待辦分類新增都共用同一份表單
